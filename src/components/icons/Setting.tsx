import { FiSettings } from "react-icons/fi";
import { IconBaseProps } from "react-icons";
import React from "react";

interface Props extends IconBaseProps {}

const Setting = (props: Props) => <FiSettings {...props} />;

export default Setting;
